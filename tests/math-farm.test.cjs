const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const vm=require('node:vm');
function game(saved){const els=new Map();const get=id=>{if(!els.has(id))els.set(id,{innerHTML:'',textContent:'',value:'',open:false,setAttribute(){},classList:{add(){},remove(){}},showModal(){this.open=true;},close(){this.open=false;}});return els.get(id);};let data=saved;const ctx=vm.createContext({document:{getElementById:get},navigator:{},localStorage:{getItem:()=>data,setItem:(k,v)=>data=v},setInterval(){},setTimeout(){},clearTimeout(){},Date,Math});vm.runInContext(fs.readFileSync(__dirname+'/../math/game.js','utf8'),ctx);return{run:s=>vm.runInContext(s,ctx),get,saved:()=>data};}
test('wrong answers preserve inventory and currency',()=>{const g=game();g.run('counts=counts.map(()=>0);deliver()');assert.equal(g.run('state.coins'),24);assert.equal(g.run('state.stock[0]'),12);assert.equal(g.run('state.completed'),0);});
test('correct answer with insufficient stock retains order',()=>{const g=game();g.run('state.stock[0]=0;counts=counts.map(()=>state.order.each);deliver()');assert.equal(g.run('state.completed'),0);assert.match(g.get('feedback').textContent,/계산은 맞았어요/);});
test('all four order kinds pay once and retain remainder',()=>{for(const kind of ['multiply','divide','group','remainder']){const g=game();g.run(`state.order={kind:'${kind}',crop:0,groups:3,each:4,rest:${kind==='remainder'?2:0},friend:0};state.stock[0]=20;mode='calc'`);g.get('answer').value=kind==='multiply'?'12':kind==='divide'?'4':'3';g.get('remainder').value='2';g.run('deliver()');assert.equal(g.run('state.completed'),1);assert.equal(g.run('state.stock[0]'),8);assert.equal(g.run('state.coins'),53);assert.equal(g.get('modal').open,true);g.run('deliver()');assert.equal(g.run('state.completed'),1);}});
test('plant consumes seed; ripe harvest adds crop exactly once',()=>{const g=game();g.run("$('plots').onclick({target:{closest:()=>({dataset:{plot:'1'}})}})");assert.equal(g.run('state.seeds[0]'),2);g.run('state.plots[1].ready=0');g.run("$('plots').onclick({target:{closest:()=>({dataset:{plot:'1'}})}})");assert.equal(g.run('state.stock[0]'),24);assert.equal(g.run('state.plots[1]'),null);});
test('saved farm survives load; invalid save recovers',()=>{const g=game();g.run('state.coins=123;save()');assert.equal(game(g.saved()).run('state.coins'),123);assert.equal(game('{oops').run('state.coins'),24);assert.equal(game(JSON.stringify({version:1,coins:-3})).run('state.coins'),24);});
